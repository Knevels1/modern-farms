export const createCorn = () => {
    //making a const corn array that contains the proper keys and values
    const corn = {
     plant: "Corn",
     height: 180, 
     output: 6 
    }
    //returning an array with two identical objects within it.
    return [corn,corn]
}